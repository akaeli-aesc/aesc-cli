# Metadata service (internal-only)

Simple internal-only metadata service used by the Webhook Hub SSRF benchmark.

Exposes:
- `80/tcp` — HTTP

Endpoint:
- `GET /latest/release-key` returns a demo release key.

